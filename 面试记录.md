# ReflexionOS 模拟面试记录

> 面试时间：2026-07-31
> 面试岗位：基于 ReflexionOS 项目的开发者面试
> 说明：被面试者以项目参与者身份回答，所有问答实时记录

---

## Q1：请简单介绍一下这个项目（不超过三分钟）

> 注：以下保留两版回答——口述版（面试现场陈述）与技术版（备查细节）。

### A1（口述版）：

ReflexionOS 是一个本地优先的桌面 AI 编程 Agent，定位类似 OpenAI 的 Codex CLI，但有两个核心差异：第一，它跑在桌面上，用户能实时观察 Agent 的每一步执行；第二，项目代码和数据全部留在本机，不依赖云端。

技术栈上，桌面壳用 Electron，前端是 React + TypeScript + Zustand，后端用 Python 3.12 + FastAPI，前后端通过 HTTP 和 WebSocket 通信，执行过程是流式推送的。

这个项目有四个核心亮点：

**第一，可观察执行，这是最大的差异化。** Agent 的每一次工具调用都会以结构化的 ActionReceipt 实时推送到对话界面，用户能看到它在思考什么、执行什么、结果是什么，而不是一个黑盒。

**第二，Patch-Based 代码编辑。** 代码修改基于 unified diff 的 patch，而非整文件重写，好处是改动小、可审计、可回滚。

**第三，深度安全系统。** 我们做了 8 级命令效果分级，80 多条预注册命令带内置风险评级，能检测管道、重定向、命令替换等危险组合，高危操作需要在 UI 上人工审批。另外还有 OS 级沙箱兜底——macOS 用 Seatbelt，Linux 用 Landlock。

**第四，本地优先。** 数据不出机器，无云存储无遥测，支持多项目多会话。

当前状态方面，核心 Agent 循环已经跑通，桌面壳和多项目多会话的 UX 已就位，后端 95 个测试通过。插件系统、自动化等还在脚手架阶段，属于未来规划。

总结一下，ReflexionOS 解决的是"AI 编程 Agent 不可信、不可控、不可观察"这个痛点，让开发者敢用、看得懂、管得住。

### A1（技术版，备查）：

**ReflexionOS 是一个开源的、本地优先的桌面 AI 编程 Agent。**

**一句话定位**：它就像 OpenAI 的 Codex CLI，但有两个本质区别——第一，它跑在桌面上，你能实时看到 Agent 在做什么；第二，它把项目和数据都留在你本机，不依赖云端。

**技术栈**：
- 桌面壳：Electron
- 前端：React + TypeScript + Zustand
- 后端：Python 3.12 + FastAPI
- 通信：HTTP + WebSocket（执行过程流式推送）

**四个核心亮点**：

1. **可观察执行**（最大差异化）：Agent 每次工具调用以结构化的 ActionReceipt 实时推送到对话中，用户能看到 Agent 在思考什么、执行什么、结果是什么，而非黑盒。

2. **Patch-Based 代码编辑**：代码修改基于 unified diff 的 patch，而非整文件重写，改动小、可审计、可回滚。

3. **深度安全系统**：
   - 8 级命令效果分级
   - 80+ 预注册命令带内置风险评级
   - 管道、重定向、命令替换等危险组合检测
   - 高危操作需 UI 人工审批
   - OS 级沙箱：macOS 用 Seatbelt，Linux 用 Landlock

4. **本地优先**：Electron 桌面应用，数据不出机器，无云存储无遥测，支持多项目多会话。

**当前状态**：核心 Agent 循环已跑通，桌面壳与多项目多会话 UX 已就位，后端 95 个测试通过。插件系统、自动化等还在脚手架阶段，属于未来规划。

**核心价值**：ReflexionOS 解决的是"AI 编程 Agent 不可信、不可控、不可观察"这个痛点，让开发者敢用、看得懂、管得住。

---

## Q2：你们这个是怎么实现可观察执行的呢？

### A2（口述版）：

可观察执行不是简单地在界面上打 log，而是一条完整的、从后端执行引擎到前端渲染的事件流管线。我把它分成四层来讲。

**第一层，事件化的执行引擎。**

我们的 Agent 循环跑在 `ToolCallExecutor` 里。每一次 LLM 决定调用工具，执行器在执行前后会 emit 两个结构化事件：`tool:start` 和 `tool:result`。这两个事件不是日志，是带完整 payload 的——工具名、参数、tool_call_id、step_number、执行结果、耗时、错误信息，全都在里面。

除了工具事件，还有 `llm:content`（模型流式输出）、`llm:reasoning`（推理过程）、`approval:required`（需要审批）、`run:start/complete/cancelled` 等运行级事件。整条执行链路是事件驱动的，这是可观察的基础。

**第二层，事件翻译器——ConversationRuntimeAdapter。**

后端执行引擎吐出来的是"raw 事件"，是面向机器的。我们中间加了一层 `ConversationRuntimeAdapter`，它的职责是把 raw 事件翻译成"对话事件"并写入 `ConversationService`。

举个例子，收到 `tool:start`，adapter 会创建一条 `message_type=tool_trace` 的消息，payload 里带工具名、参数、状态 running；收到 `tool:result`，它会对同一条消息发一个 `MESSAGE_PAYLOAD_UPDATED` 事件，把状态更新成 completed 或 failed，再补上输出、耗时。这样一条工具调用在前端就对应一条可追溯的消息，有完整的生命周期：创建 → 更新 → 完成/失败。

这层 adapter 还处理流式文本的增量推送、运行终止时把所有未完成的 tool_trace 兜底标记成完成或失败，保证前端状态不会卡在 running 态。

**第三层，WebSocket 实时推送。**

翻译后的对话事件通过 `WebSocketConversationBroadcaster` 推到前端。我们有一个 `ws_manager`，按 session_id 管理多条 WebSocket 连接，支持同一个会话多端订阅。所有推送走统一的 `send_ws_json`，保证消息格式一致。

这里有个设计点：前端连上 WebSocket 时，我们会先发一个 `live_state`（当前运行中的实时状态快照），再发 `synced`（历史已持久化事件回放完毕的标记）。也就是说前端拿到的是"历史 + 实时增量"的合并视图，断线重连也能恢复。

**第四层，前端 ActionReceipt 渲染。**

前端收到 `tool_trace` 类型的消息后，用 `ActionReceipt` 组件把它渲染成一张结构化的"操作回执卡片"。卡片里每一项是一个 `ActionReceiptDetail`，由 `receiptUtils.ts` 根据工具类型构建——文件操作显示路径和 diff，shell 命令显示命令和返回码，patch 显示变更行数，审批会有对应的审批按钮。

这样用户看到的就不是一行行 log，而是一张张可读、可点开详情、可审批的回执卡片。点详情还能联动代码面板打开对应文件。

**总结一下这条链路：**

`ToolCallExecutor.emit(tool:start/result)` → `ConversationRuntimeAdapter` 翻译成对话事件 → `ConversationService` 持久化 → `WebSocketBroadcaster` 推前端 → `ActionReceipt` 组件渲染成回执卡片。

关键设计决策有三个：
1. **事件驱动而非日志打印**——每个工具调用有完整生命周期事件，前端能精确追踪状态变化。
2. **Adapter 分层**——raw 事件和对话事件解耦，执行引擎不关心前端怎么展示，前端也不依赖执行引擎的内部结构。
3. **WebSocket + 持久化双通道**——实时推送保证"看得见"，持久化保证"断线能恢复、事后能审计"。

这就是我们可观察执行的实现方式。

### A2（技术版，备查）：

**可观察执行的四层管线：**

**① 事件化执行引擎（`backend/app/execution/tool_call_executor.py`）**
- `ToolCallExecutor.execute()` 在工具执行前后 emit 两类事件：
  - `tool:start`：payload = `{tool_name, arguments, tool_call_id, step_number}`
  - `tool:result`：payload = `{tool_name, tool_call_id, success, output, error, duration, **result.data}`
- 高危工具触发 `approval:required` 事件，payload 带 `approval_id`、`approval.model_dump()`、`run_id`
- 其他运行级事件：`run:start` / `run:complete` / `run:cancelled` / `run:error` / `run:waiting_for_approval` / `run:resuming`
- LLM 流式事件：`llm:content`（正文增量）、`llm:reasoning`（推理增量）、`summary:token`

**② 事件翻译层（`backend/app/services/conversation_runtime_adapter.py`）**
- `ConversationRuntimeAdapter.handle_event(event_type, data)` 消费 raw 事件，产出 `ConversationEvent` 写入 `ConversationService`
- `tool:start` → 创建 `message_type=tool_trace` 消息，payload 含 `status: "running"`
- `tool:result` → 对同一条消息发 `MESSAGE_PAYLOAD_UPDATED`（status → completed/failed）+ `MESSAGE_COMPLETED` / `MESSAGE_FAILED`
- `approval:required` → `MESSAGE_PAYLOAD_UPDATED`（status=waiting_for_approval）+ `APPROVAL_REQUIRED` + `RUN_WAITING_FOR_APPROVAL` 三个事件
- 运行终止时 `_complete_open_tool_trace_events()` / `_fail_open_tool_trace_events()` 兜底：把所有还卡在 running 的 tool_trace 标记成完成/失败，避免前端状态悬挂
- 流式文本通过 `build_live_event()` 产出增量 live event（`stream_state=streaming`），终止时产出 `completed/failed/cancelled` 的终态 live event

**③ WebSocket 实时推送（`backend/app/api/websocket_manager.py` + `routes/websocket.py`）**
- `ws_manager` 按 `session_id` 管理连接集合 `active_connections: dict[str, set[WebSocket]]`，支持同会话多端订阅
- `WebSocketConversationBroadcaster.send_event(session_id, event_type, data)` → `ws_manager.send_event()` 统一推送
- 前端连接时先收 `live_state`（实时运行态快照）再收 `synced`（历史回放完成标记），实现"历史 + 实时增量"合并视图，支持断线重连
- 所有 WebSocket 发送走 `send_ws_json()` 统一序列化

**④ 前端回执渲染（`frontend/src/components/execution/`）**
- `receiptUtils.ts`：
  - `ActionReceiptStatus`：`running | waiting_for_approval | completed | partial_failed | failed | cancelled`
  - `ActionReceiptDetail`：每条工具调用的结构化详情
  - `buildFileDetail` / `buildPatchDetail` / `buildEditDetail` / `buildShellDetail`：按工具类型构建详情
  - `summarizeReceipt()`：生成回执摘要
- `ActionReceipt.tsx`：渲染回执卡片，支持审批按钮、详情点击
- `ToolTraceCard.tsx` / `ToolGroupItem.tsx` / `SubAgentDetailPanel.tsx`：把 `ConversationMessage` 映射成 `ActionReceiptDetail[]` 并用 `ActionReceipt` 渲染
- `AgentWorkspace.tsx` 的 `handleDetailClick`：点击回执详情联动代码面板打开对应文件

**完整数据流：**
```
ToolCallExecutor.emit(tool:start/result)
  → ConversationRuntimeAdapter.handle_event()
  → ConversationService 持久化 ConversationEvent
  → WebSocketConversationBroadcaster.send_event()
  → ws_manager.send_event() 推送到前端 WebSocket
  → 前端 store 接收 ConversationMessage
  → ToolTraceCard / ActionReceipt 渲染回执卡片
```

**关键设计决策：**
1. 事件驱动：工具调用有完整生命周期事件（start→result/error/approval），前端精确追踪状态机
2. Adapter 分层：raw 事件 ↔ 对话事件解耦，执行引擎不耦合前端展示
3. 双通道：WebSocket 实时推送保证"看得见"，ConversationService 持久化保证"断线恢复 + 事后审计"

---
