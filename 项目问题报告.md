# ReflexionOS 项目问题报告与改进建议

最初生成：2026-06-11 ~ 2026-06-12（合并自 BUG_REPORT.md、research_findings.md、problem.txt）
本次复核：2026-07-23

> 说明：本报告是历次排查笔记的汇总整理版，已核实每条问题当前是否仍然成立，过时/已修复的内容不再保留。

---

## 一、待处理问题（复核后确认仍存在）

### 🔴 高优先级

#### 1. CORS 配置过宽
**位置**：`backend/app/main.py:108-114`
```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_credentials=False,
    allow_methods=["*"],
    allow_headers=["*"],
)
```
**现状**：`allow_credentials` 已经是 `False`（早期报告曾记为 `True`，风险已降低），但 `allow_origins=["*"]` 仍未收紧。
**背景**：后端固定监听 `127.0.0.1:8000`（仅本机 loopback，见 `frontend/electron/backend-manager.cjs:7-8`），非公网暴露，实际攻击面有限；但生产环境 Electron 用 `file://` 协议加载渲染页面，开发环境用 Vite dev server（如 `http://localhost:5173`），要收紧白名单需要先确认 `file://` 场景下实际发出的 Origin 值，避免改错导致生产环境请求被拦截。
**状态**：待排期处理，需要先验证 Origin 值再改。

#### 2. Python 版本要求不一致
**问题**：`backend/pyproject.toml:requires-python` 为 `>=3.11`，但 `README.md` 多处标注 `Python 3.12`（含 conda/venv 创建命令）。
**建议**：统一为 `>=3.12` 或明确实际支持的最低版本。

### 🟡 中优先级

#### 3. 依赖版本管理问题
- `backend/requirements.txt:13-15`：`tiktoken`、`gitpython`、`pyyaml` 三个依赖仍未固定版本号，构建不可重现。
- `frontend/package.json:71`：`axios` 声明 `^1.6.7`，实际安装 `1.17.0`（复核时点），版本跨度较大，需评估是否有破坏性变更影响。

#### 4. 前后端版本号不同步
`frontend/package.json` 当前为 `1.1.0`，`backend/pyproject.toml` 为 `0.1.0`。若无统一版本管理策略，建议明确前后端版本号是否需要对齐。

#### 5. 过度使用通用异常捕获
`backend/app/**/*.py` 中 `except Exception:` 复核时点共 99 处（早期报告记为 32 处，数量持续增长），掩盖具体错误类型，增加排查难度。建议对关键路径逐步替换为具体异常类型。

#### 6. WebSocket 无自动重连机制
前端 WebSocket 连接断开后需用户手动刷新页面，未做自动重连。

#### 7. 数据库事务管理边界不清晰
多处手动传递 `db_session`，缺乏统一的事务边界管理，容易漏提交/漏回滚。

### 🔵 低优先级

#### 8. 代码风格问题（Ruff）
早期扫描发现 100+ 风格问题（行长度、import 顺序、旧式类型注解、未使用变量等）。是否已随后续开发自然收敛未复核，如仍有价值可执行：
```bash
cd backend && ruff check . --fix && ruff format .
```

#### 9. 类型注解与错误处理不一致
部分函数参数缺少类型注解；`ValueError` 与项目自定义 `AppError` 混用，缺乏统一规范。

---

## 二、产品/架构层面的开放问题（来自 problem.txt，未展开排查，待讨论）

1. **`context_assembler` 与 `prompt_manager` 的职责边界**：两者功能是否有冲突或重叠？`build_for_session` 的职责是否与其他模块重复，能否合并/删除？需要结合当前代码实现具体梳理。
2. **Agent Loop 的长任务记忆问题**：面对需要读取大量文件并操作的复杂任务时，模型会遗忘之前读取的内容。已有的 `recall_tool` 效果不足。需要对比成熟商业产品（如 Claude Code、Cursor 等）的上下文管理方案，分析可行的优化方向（如滚动摘要、分层记忆、显式的文件读取状态跟踪等）。

---

## 三、前端 Store 组织方式研究结论（来自 research_findings.md，供后续重构参考）

**现状问题**：
1. `/stores` 与 `/features/*/` 混合存在，分类标准不清晰。
2. 文件命名不统一（如 `projectStore.ts` vs `conversationStore.ts`）。
3. 新增 store 时不清楚应放在哪个目录。

**业界主流方案调研**（Bulletproof React、Plane.so、Excalidraw、Redux Toolkit、Cal.com）总结出两种命名惯例：`{entity}.store.ts`（MobX/自定义状态管理常见）和 `{entity}Slice.ts`（Redux Toolkit 惯例）。

**推荐方向**：功能域内嵌 + 统一命名——业务相关 store 放入对应 `features/{domain}/stores/`，仅纯 UI 状态（主题、toast、动画）放在 `shared/stores/`；文件统一 `{entity}.store.ts` 后缀。

**迁移检查清单**（若决定推进该重构）：
- [ ] 创建 `shared/stores/` 目录，迁移 UI 相关 store（theme、toast、animation）
- [ ] 在各 feature 下建立 `stores/` 子目录，迁移对应业务 store
- [ ] 统一重命名为 `.store.ts` 后缀
- [ ] 更新所有导入路径及 TypeScript path mapping
- [ ] 构建验证 + 更新相关文档

参考资料：[Bulletproof React](https://github.com/alan2207/bulletproof-react)、[Plane Architecture](https://github.com/makeplane/plane)、[Redux Style Guide](https://redux.js.org/style-guide/)、[Zustand Best Practices](https://github.com/pmndrs/zustand)

---

## 四、已确认过时 / 已解决，不再跟踪的问题

以下内容来自早期排查笔记，本次复核确认已解决或前提已失效，仅存档说明，不再作为待办：

- ~~Python 3.9 不支持 match 语句（conversation_projection.py:48）~~ —— 当时诊断环境是 Python 3.9.6，与项目要求的 `>=3.11` 不符，属环境问题非代码问题；现行环境下 `match` 语句无问题。
- ~~agent_service.py logger 未定义引用~~ —— 已修复。
- ~~WebSocket 消息处理缺少 return~~ —— 已修复。
- ~~session_service 更新逻辑冗余（exclude_unset 与 .get 回退冲突）~~ —— 已修复。
- ~~conversation_service.py:463 调试 print 残留~~ —— 已修复。
- ~~缺少 `.env.example` 模板~~ —— `backend/.env.example` 已存在。
- ~~openai_adapter.py 硬编码 placeholder API key~~ —— 复核时未在代码中找到该硬编码写法。
- ~~数据库路径硬编码在 alembic.ini~~ —— 未在本次复核范围内重新验证，若后续发现仍存在可重新立项。
